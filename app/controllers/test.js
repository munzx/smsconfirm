'use strict';

//Dependencies
var fs = require("fs"),
	mongoose = require('mongoose'),
	_ = require('lodash'),
	errorHandler = require('./error');

module.exports.index = function (req, res, next) {
	
}